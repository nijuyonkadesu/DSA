#ifndef COMMON_H
#define COMMON_H
// This is the encaptulation of all common header files
// need by almost all cpp files.

#include <map>
#include <set>
#include <vector>
#include <set>
#include <iostream>
#include <cassert>
#include<string>
#include<cstdlib>

using namespace std;

typedef unsigned state;
typedef char input;

enum {EPS = -1, NONE = 0};

#endif
